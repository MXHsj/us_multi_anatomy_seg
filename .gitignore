.DS*
__pycache__/
*.pyc

# local secrets (HF_TOKEN, etc.) -- never commit
.env

# pretrained weights
*.pth
*.pt
# Note: work_dir/UltraSam/ is tracked (README.md), but its UltraSam.pth weight
# stays ignored via the global *.pth rule above and is auto-downloaded at runtime.

# raw/local dataset materializations
datasets/TNSC2020/
datasets/BLUSG/
datasets/BUSBRA/
datasets/BUSI/
datasets/OKU/
datasets/CAMUS/
datasets/FTP/
datasets/UltraBones100k/
datasets/AULID/
datasets/UNS/
datasets/RobLUS/
.cache/
data_cache/

# generated benchmark artifacts
# results/
*.zip
presentation
# results/**/visualizations/
results/**/coco_export/
# latex
ASMUS2026_Evaluating_Ultrasound_Segmentation_Models_MONAI_US_WG/

# IDEs
.vscode/

# per-shard outputs are redundant once merged into the parent run dir
results/**/_shards/

# Medical SAM3 source clone (throwaway; install the `sam3` package instead)
medical-sam3/
# scratch dataset download zips (wget -c staging)
downloads/
# slurm logs and stray job stdout/stderr
logs/
msam3_*.out
msam3_*.err
# large reference docs (e.g. the paper PDF) are not committed
*.pdf
